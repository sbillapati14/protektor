{
  "name": "protektor",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "mocha --timeout 5000 --require babel-core/register test/*.test.js",
    "prebuild": "rimraf dist",
    "build": "npm-run-all --parallel build:*",
    "build:main": "babel --copy-files --out-dir dist src",
    "build:umd": "webpack --mode production --output-filename protektor.js",
    "build:umd.min": "webpack --mode production --output-filename protektor.min.js -p"
  },
  "babel": {
    "presets": [
      "es2015",
      "stage-2"
    ]
  },
  "keywords": [],
  "author": "Rob Sliwa",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-loader": "^7.1.4",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "chai": "^4.1.2",
    "concurrently": "^3.6.0",
    "mocha": "^5.2.0",
    "npm-run-all": "^4.1.3",
    "rimraf": "^2.6.2",
    "webpack": "^4.14.0",
    "webpack-command": "^0.3.1"
  }
}
